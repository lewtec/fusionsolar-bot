[tools]
go = "1.25.7"
"github:lucasew/workspaced" = "0.4.5"

[tasks.install]
depends = ["install:*"]

[tasks."install:go"]
run = "go mod download"

[tasks.lint]
run = "workspaced codebase lint"

[tasks.fmt]
run = "workspaced codebase format"

[tasks.test]
depends = ["install:*"]
run = "go test ./..."

[tasks.codegen]
depends = ["install:*"]
run = "go generate ./..."

[tasks.ci]
depends = ["lint", "test"]
